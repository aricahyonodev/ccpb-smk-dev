import React from 'react'

const SubHeading1Medium = ({ children, className }) => {
  return <p className={`text-[20px] font-medium ${className}`}>{children}</p>;
};

export default SubHeading1Medium;